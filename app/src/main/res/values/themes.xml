<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EMinesEmployee" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorAccent">@color/primary_color</item>
        <item name="colorPrimaryDark">@color/status_bar_color_1</item>
        <item name="colorSecondary">@color/status_bar_color_1</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:screenOrientation">portrait</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>

        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.EMinesEmployee" parent="Base.Theme.EMinesEmployee" />
    <!-- Base application theme. -->
    <style name="Base.MyAppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="MyAppTheme" parent="Base.MyAppTheme" />
</resources>